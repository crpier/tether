// Do not edit — generated by `just codegen`.
// Source: openapi.json.

import createClient, { type ClientOptions } from "openapi-fetch";

import type { paths } from "./schema";

export type TetherClient = ReturnType<typeof createTetherClient>;

export function createTetherClient(options: ClientOptions = {}) {
  return createClient<paths>({
    baseUrl: "/",
    credentials: "include",
    ...options,
  });
}
