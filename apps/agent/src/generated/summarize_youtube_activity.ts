// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const summarize_youtube_activityParameters = Type.Object({
  after: Type.String({ format: "date-time" }),
  before: Type.String({ format: "date-time" }),
});

export type SummarizeYouTubeActivityParams = Static<
  typeof summarize_youtube_activityParameters
>;

export const summarize_youtube_activityTool: ToolDefinition<
  typeof summarize_youtube_activityParameters,
  TetherToolDetails
> = {
  name: "summarize_youtube_activity",
  label: "SummarizeYoutubeActivity",
  description:
    "Summarize liked videos in a half-open interval as a viewing proxy.\n\nThe returned duration is the sum of full video lengths, not measured watch\ntime. Use timezone-aware boundaries so relative calendar weeks reflect the\nuser's local timezone.",
  promptSnippet:
    "Summarize liked videos in a half-open interval as a viewing proxy.\n\nThe returned duration is the sum of full video lengths, not measured watch\ntime. Use timezone-aware boundaries so relative calendar weeks reflect the\nuser's local timezone.",
  parameters: summarize_youtube_activityParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/summarize_youtube_activity",
        name: "summarize_youtube_activity",
      },
      params,
      signal,
    );
  },
};

export function registerSummarizeYoutubeActivityTool(pi: ExtensionAPI): void {
  pi.registerTool(summarize_youtube_activityTool);
}

export default function summarize_youtube_activityToolExtension(
  pi: ExtensionAPI,
): void {
  registerSummarizeYoutubeActivityTool(pi);
}
