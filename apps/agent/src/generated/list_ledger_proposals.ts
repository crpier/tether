// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_ledger_proposalsParameters = Type.Object({});

export type ListLedgerProposalsParams = Static<
  typeof list_ledger_proposalsParameters
>;

export const list_ledger_proposalsTool: ToolDefinition<
  typeof list_ledger_proposalsParameters,
  TetherToolDetails
> = {
  name: "list_ledger_proposals",
  label: "ListLedgerProposals",
  description:
    "List pending Ledger definitions awaiting explicit user approval.",
  promptSnippet:
    "List pending Ledger definitions awaiting explicit user approval.",
  parameters: list_ledger_proposalsParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/list_ledger_proposals",
        name: "list_ledger_proposals",
      },
      params,
      signal,
    );
  },
};

export function registerListLedgerProposalsTool(pi: ExtensionAPI): void {
  pi.registerTool(list_ledger_proposalsTool);
}

export default function list_ledger_proposalsToolExtension(
  pi: ExtensionAPI,
): void {
  registerListLedgerProposalsTool(pi);
}
