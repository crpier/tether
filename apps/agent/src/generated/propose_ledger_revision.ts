// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const propose_ledger_revisionParameters = Type.Object({
  fields: Type.Array(
    Type.Object({
      deprecated: Type.Optional(Type.Boolean({ default: false })),
      description: Type.String({ minLength: 1 }),
      enum_values: Type.Optional(
        Type.Array(Type.String(), { maxItems: 64, minItems: 1 }),
      ),
      field_id: Type.String({ minLength: 1 }),
      label: Type.String({ minLength: 1 }),
      required: Type.Boolean(),
      type: StringEnum([
        "boolean",
        "date",
        "datetime",
        "decimal",
        "enum",
        "integer",
        "text",
      ] as const),
      unit: Type.Optional(Type.String({ minLength: 1 })),
    }),
    { maxItems: 32, minItems: 1 },
  ),
  name: Type.String({ minLength: 1 }),
  purpose: Type.String({ minLength: 1 }),
  status: Type.Optional(
    StringEnum(["active", "completed", "abandoned"] as const),
  ),
  ledger_id: Type.String({ format: "uuid" }),
  revision: Type.Integer({ exclusiveMinimum: 0 }),
});

export type ProposeLedgerRevisionParams = Static<
  typeof propose_ledger_revisionParameters
>;

export const propose_ledger_revisionTool: ToolDefinition<
  typeof propose_ledger_revisionParameters,
  TetherToolDetails
> = {
  name: "propose_ledger_revision",
  label: "ProposeLedgerRevision",
  description:
    "Propose an exact successor to one observed active Ledger revision.",
  promptSnippet:
    "Propose an exact successor to one observed active Ledger revision.",
  parameters: propose_ledger_revisionParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/propose_ledger_revision",
        name: "propose_ledger_revision",
      },
      params,
      signal,
    );
  },
};

export function registerProposeLedgerRevisionTool(pi: ExtensionAPI): void {
  pi.registerTool(propose_ledger_revisionTool);
}

export default function propose_ledger_revisionToolExtension(
  pi: ExtensionAPI,
): void {
  registerProposeLedgerRevisionTool(pi);
}
