// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const add_bookParameters = Type.Object({
  title: Type.String(),
  intent_context: Type.String(),
  author: Type.Optional(Type.String()),
});

export type AddBookParams = Static<typeof add_bookParameters>;

export const add_bookTool: ToolDefinition<
  typeof add_bookParameters,
  TetherToolDetails
> = {
  name: "add_book",
  label: "AddBook",
  description: "Params for Adding a `book` Bucket item.",
  promptSnippet: "Params for Adding a `book` Bucket item.",
  parameters: add_bookParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/add_book", name: "add_book" },
      params,
      signal,
    );
  },
};

export function registerAddBookTool(pi: ExtensionAPI): void {
  pi.registerTool(add_bookTool);
}

export default function add_bookToolExtension(pi: ExtensionAPI): void {
  registerAddBookTool(pi);
}
