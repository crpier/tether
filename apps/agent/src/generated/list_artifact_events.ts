// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_artifact_eventsParameters = Type.Object({
  artifact_id: Type.String({ format: "uuid" }),
});

export type ListArtifactEventsParams = Static<
  typeof list_artifact_eventsParameters
>;

export const list_artifact_eventsTool: ToolDefinition<
  typeof list_artifact_eventsParameters,
  TetherToolDetails
> = {
  name: "list_artifact_events",
  label: "ListArtifactEvents",
  description: "Params for listing an artifact's events, oldest first.",
  promptSnippet: "Params for listing an artifact's events, oldest first.",
  parameters: list_artifact_eventsParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/list_artifact_events",
        name: "list_artifact_events",
      },
      params,
      signal,
    );
  },
};

export function registerListArtifactEventsTool(pi: ExtensionAPI): void {
  pi.registerTool(list_artifact_eventsTool);
}

export default function list_artifact_eventsToolExtension(
  pi: ExtensionAPI,
): void {
  registerListArtifactEventsTool(pi);
}
