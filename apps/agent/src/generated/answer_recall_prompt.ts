// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const answer_recall_promptParameters = Type.Object({
  prompt_id: Type.String({ format: "uuid" }),
  selected_index: Type.Integer(),
  response_ms: Type.Integer(),
});

export type AnswerRecallPromptParams = Static<
  typeof answer_recall_promptParameters
>;

export const answer_recall_promptTool: ToolDefinition<
  typeof answer_recall_promptParameters,
  TetherToolDetails
> = {
  name: "answer_recall_prompt",
  label: "AnswerRecallPrompt",
  description:
    "Params for answering a recall prompt: the chosen option and elapsed time.",
  promptSnippet:
    "Params for answering a recall prompt: the chosen option and elapsed time.",
  parameters: answer_recall_promptParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/answer_recall_prompt",
        name: "answer_recall_prompt",
      },
      params,
      signal,
    );
  },
};

export function registerAnswerRecallPromptTool(pi: ExtensionAPI): void {
  pi.registerTool(answer_recall_promptTool);
}

export default function answer_recall_promptToolExtension(
  pi: ExtensionAPI,
): void {
  registerAnswerRecallPromptTool(pi);
}
