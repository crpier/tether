// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const create_todoParameters = Type.Object({
  action: Type.String(),
  condition: Type.Optional(Type.String()),
});

export type CreateTodoParams = Static<typeof create_todoParameters>;

export const create_todoTool: ToolDefinition<
  typeof create_todoParameters,
  TetherToolDetails
> = {
  name: "create_todo",
  label: "CreateTodo",
  description:
    'Params for capturing a one-off actionable task.\n\n`condition` is an optional free-text waiting condition ("next time I visit\nAna") for an event-triggered task that resists a fixed date; omit it for a\ntask that is ready now. Attach a deadline trigger afterward with\n`link_todo_trigger`.',
  promptSnippet:
    'Params for capturing a one-off actionable task.\n\n`condition` is an optional free-text waiting condition ("next time I visit\nAna") for an event-triggered task that resists a fixed date; omit it for a\ntask that is ready now. Attach a deadline trigger afterward with\n`link_todo_trigger`.',
  parameters: create_todoParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/create_todo", name: "create_todo" },
      params,
      signal,
    );
  },
};

export function registerCreateTodoTool(pi: ExtensionAPI): void {
  pi.registerTool(create_todoTool);
}

export default function create_todoToolExtension(pi: ExtensionAPI): void {
  registerCreateTodoTool(pi);
}
