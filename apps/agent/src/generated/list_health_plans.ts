// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_health_plansParameters = Type.Object({
  limit: Type.Optional(Type.Integer({ default: 50, maximum: 100, minimum: 1 })),
});

export type ListHealthPlansParams = Static<typeof list_health_plansParameters>;

export const list_health_plansTool: ToolDefinition<
  typeof list_health_plansParameters,
  TetherToolDetails
> = {
  name: "list_health_plans",
  label: "ListHealthPlans",
  description:
    "List a bounded set of current active and paused exercise intentions.",
  promptSnippet:
    "List a bounded set of current active and paused exercise intentions.",
  parameters: list_health_plansParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/list_health_plans",
        name: "list_health_plans",
      },
      params,
      signal,
    );
  },
};

export function registerListHealthPlansTool(pi: ExtensionAPI): void {
  pi.registerTool(list_health_plansTool);
}

export default function list_health_plansToolExtension(pi: ExtensionAPI): void {
  registerListHealthPlansTool(pi);
}
