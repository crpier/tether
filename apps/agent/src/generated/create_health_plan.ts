// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const create_health_planParameters = Type.Object({
  exercise_types: Type.Array(
    StringEnum([
      "running",
      "strength_training",
      "walking",
      "weightlifting",
    ] as const),
  ),
  grace_minutes: Type.Optional(
    Type.Integer({ default: 60, maximum: 360, minimum: 15 }),
  ),
  timezone: Type.String({ minLength: 1 }),
  title: Type.String({ minLength: 1 }),
  windows: Type.Array(
    Type.Object({
      end_local_time: Type.String({ format: "time" }),
      start_local_time: Type.String({ format: "time" }),
      weekday: StringEnum([
        "monday",
        "tuesday",
        "wednesday",
        "thursday",
        "friday",
        "saturday",
        "sunday",
      ] as const),
    }),
  ),
});

export type HealthPlanDraft = Static<typeof create_health_planParameters>;

export const create_health_planTool: ToolDefinition<
  typeof create_health_planParameters,
  TetherToolDetails
> = {
  name: "create_health_plan",
  label: "CreateHealthPlan",
  description:
    "Create one explicitly requested recurring exercise intention.\n\nUse an IANA `timezone` and one or more same-day local weekly `windows`.\n`strength_training` accepts both strength-training and weightlifting Health\nConnect episodes. The host rejects creation or revision unless the active\nturn has fresh foreground user Evidence.",
  promptSnippet:
    "Create one explicitly requested recurring exercise intention.\n\nUse an IANA `timezone` and one or more same-day local weekly `windows`.\n`strength_training` accepts both strength-training and weightlifting Health\nConnect episodes. The host rejects creation or revision unless the active\nturn has fresh foreground user Evidence.",
  parameters: create_health_planParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/create_health_plan",
        name: "create_health_plan",
      },
      params,
      signal,
    );
  },
};

export function registerCreateHealthPlanTool(pi: ExtensionAPI): void {
  pi.registerTool(create_health_planTool);
}

export default function create_health_planToolExtension(
  pi: ExtensionAPI,
): void {
  registerCreateHealthPlanTool(pi);
}
