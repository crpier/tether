// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const appendParameters = Type.Object({
  memory_id: Type.String({ format: "uuid" }),
  content: Type.String({ minLength: 1 }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
});

export type AppendParams = Static<typeof appendParameters>;

export const appendTool: ToolDefinition<
  typeof appendParameters,
  TetherToolDetails
> = {
  name: "append",
  label: "Append",
  description:
    "Params for appending agent-routed verbatim content to a Memory.",
  promptSnippet:
    "Params for appending agent-routed verbatim content to a Memory.",
  parameters: appendParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/append", name: "append" },
      params,
      signal,
    );
  },
};

export function registerAppendTool(pi: ExtensionAPI): void {
  pi.registerTool(appendTool);
}

export default function appendToolExtension(pi: ExtensionAPI): void {
  registerAppendTool(pi);
}
