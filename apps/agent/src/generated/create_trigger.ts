// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const create_triggerParameters = Type.Object({
  recurrence: StringEnum(["once", "daily", "weekly"] as const),
  action_kind: StringEnum(["message", "prompt"] as const),
  payload: Type.String(),
  timezone: Type.Optional(Type.String()),
  time_of_day: Type.Optional(Type.String()),
  weekday: Type.Optional(Type.Integer()),
  fire_at: Type.Optional(Type.String({ format: "date-time" })),
});

export type CreateTriggerParams = Static<typeof create_triggerParameters>;

export const create_triggerTool: ToolDefinition<
  typeof create_triggerParameters,
  TetherToolDetails
> = {
  name: "create_trigger",
  label: "CreateTrigger",
  description:
    "Params for scheduling a trigger.\n\n`once` carries an absolute `fire_at`; `daily`/`weekly` carry `timezone` and\n`time_of_day` (and a `weekday` for weekly). Mismatched fields are rejected\nas a well-formed `invalid_input` envelope, never a corrupt row.",
  promptSnippet:
    "Params for scheduling a trigger.\n\n`once` carries an absolute `fire_at`; `daily`/`weekly` carry `timezone` and\n`time_of_day` (and a `weekday` for weekly). Mismatched fields are rejected\nas a well-formed `invalid_input` envelope, never a corrupt row.",
  parameters: create_triggerParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/create_trigger", name: "create_trigger" },
      params,
      signal,
    );
  },
};

export function registerCreateTriggerTool(pi: ExtensionAPI): void {
  pi.registerTool(create_triggerTool);
}

export default function create_triggerToolExtension(pi: ExtensionAPI): void {
  registerCreateTriggerTool(pi);
}
