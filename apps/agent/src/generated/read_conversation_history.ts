// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const read_conversation_historyParameters = Type.Object({
  limit: Type.Optional(Type.Integer({ default: 20 })),
  before: Type.Optional(Type.Integer()),
});

export type ReadConversationHistoryParams = Static<
  typeof read_conversation_historyParameters
>;

export const read_conversation_historyTool: ToolDefinition<
  typeof read_conversation_historyParameters,
  TetherToolDetails
> = {
  name: "read_conversation_history",
  label: "ReadConversationHistory",
  description:
    "Params for reading transcript rows from before the live pi session.\n\n`limit` windows to the newest `limit` prior-session rows (capped at 50 so\na single call can't flood the model's context); `before` is an optional\n`seq` cursor for paging further back once the first page has been read.",
  promptSnippet:
    "Params for reading transcript rows from before the live pi session.\n\n`limit` windows to the newest `limit` prior-session rows (capped at 50 so\na single call can't flood the model's context); `before` is an optional\n`seq` cursor for paging further back once the first page has been read.",
  parameters: read_conversation_historyParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/read_conversation_history",
        name: "read_conversation_history",
      },
      params,
      signal,
    );
  },
};

export function registerReadConversationHistoryTool(pi: ExtensionAPI): void {
  pi.registerTool(read_conversation_historyTool);
}

export default function read_conversation_historyToolExtension(
  pi: ExtensionAPI,
): void {
  registerReadConversationHistoryTool(pi);
}
