// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const promote_gmail_evidenceParameters = Type.Object({
  claim_hint: Type.String({ minLength: 1 }),
  message_id: Type.String({ minLength: 1 }),
});

export type PromoteGmailEvidenceParams = Static<
  typeof promote_gmail_evidenceParameters
>;

export const promote_gmail_evidenceTool: ToolDefinition<
  typeof promote_gmail_evidenceParameters,
  TetherToolDetails
> = {
  name: "promote_gmail_evidence",
  label: "PromoteGmailEvidence",
  description:
    "Promote one active-turn Gmail read under explicit remember authorization.",
  promptSnippet:
    "Promote one active-turn Gmail read under explicit remember authorization.",
  parameters: promote_gmail_evidenceParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/promote_gmail_evidence",
        name: "promote_gmail_evidence",
      },
      params,
      signal,
    );
  },
};

export function registerPromoteGmailEvidenceTool(pi: ExtensionAPI): void {
  pi.registerTool(promote_gmail_evidenceTool);
}

export default function promote_gmail_evidenceToolExtension(
  pi: ExtensionAPI,
): void {
  registerPromoteGmailEvidenceTool(pi);
}
