// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const proposeParameters = Type.Object({
  consumer: Type.String(),
  title: Type.String(),
  summary: Type.String(),
  actions: Type.Array(
    Type.Object({
      kind: Type.String(),
      scope: Type.Optional(Type.String()),
      params: Type.Optional(Type.Record(Type.String(), Type.Unknown())),
    }),
  ),
});

export type ProposeParams = Static<typeof proposeParameters>;

export const proposeTool: ToolDefinition<
  typeof proposeParameters,
  TetherToolDetails
> = {
  name: "propose",
  label: "Propose",
  description:
    "Params for composing a proposal — an explicit set of typed actions.\n\nThe proposal executes immediately only if every action is covered by an\nexisting autonomy grant; otherwise the whole set queues for human review.\nApproval, rejection, and granting are human-only and never tools.",
  promptSnippet:
    "Params for composing a proposal — an explicit set of typed actions.\n\nThe proposal executes immediately only if every action is covered by an\nexisting autonomy grant; otherwise the whole set queues for human review.\nApproval, rejection, and granting are human-only and never tools.",
  parameters: proposeParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/propose", name: "propose" },
      params,
      signal,
    );
  },
};

export function registerProposeTool(pi: ExtensionAPI): void {
  pi.registerTool(proposeTool);
}

export default function proposeToolExtension(pi: ExtensionAPI): void {
  registerProposeTool(pi);
}
