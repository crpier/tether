// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const search_youtubeParameters = Type.Object({
  q: Type.String(),
  limit: Type.Optional(Type.Integer({ default: 50, exclusiveMinimum: 0 })),
});

export type SearchYouTubeParams = Static<typeof search_youtubeParameters>;

export const search_youtubeTool: ToolDefinition<
  typeof search_youtubeParameters,
  TetherToolDetails
> = {
  name: "search_youtube",
  label: "SearchYoutube",
  description:
    "Search saved videos by title, description, and transcript text in one pass.\n\nEach result row already carries a description (and, for transcript matches, a\nsnippet), so a single query is usually enough — prefer reading the rows over\nre-searching with reworded terms.",
  promptSnippet:
    "Search saved videos by title, description, and transcript text in one pass.\n\nEach result row already carries a description (and, for transcript matches, a\nsnippet), so a single query is usually enough — prefer reading the rows over\nre-searching with reworded terms.",
  parameters: search_youtubeParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/search_youtube", name: "search_youtube" },
      params,
      signal,
    );
  },
};

export function registerSearchYoutubeTool(pi: ExtensionAPI): void {
  pi.registerTool(search_youtubeTool);
}

export default function search_youtubeToolExtension(pi: ExtensionAPI): void {
  registerSearchYoutubeTool(pi);
}
