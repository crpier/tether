// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const editParameters = Type.Object({
  memory_id: Type.String({ format: "uuid" }),
  content: Type.String({ minLength: 1 }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
  facets: Type.Optional(Type.Record(Type.String(), Type.String())),
});

export type EditParams = Static<typeof editParameters>;

export const editTool: ToolDefinition<
  typeof editParameters,
  TetherToolDetails
> = {
  name: "edit",
  label: "Edit",
  description:
    "Params for editing a Memory's content at an observed version.\n\n`facets`, when supplied, replaces the stored Commons facet set verbatim\n(an empty object clears it); omitted, facets stay unchanged.",
  promptSnippet:
    "Params for editing a Memory's content at an observed version.\n\n`facets`, when supplied, replaces the stored Commons facet set verbatim\n(an empty object clears it); omitted, facets stay unchanged.",
  parameters: editParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/edit", name: "edit" },
      params,
      signal,
    );
  },
};

export function registerEditTool(pi: ExtensionAPI): void {
  pi.registerTool(editTool);
}

export default function editToolExtension(pi: ExtensionAPI): void {
  registerEditTool(pi);
}
