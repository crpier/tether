// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_gmail_labelsParameters = Type.Object({});

export type ListGmailLabelsParams = Static<typeof list_gmail_labelsParameters>;

export const list_gmail_labelsTool: ToolDefinition<
  typeof list_gmail_labelsParameters,
  TetherToolDetails
> = {
  name: "list_gmail_labels",
  label: "ListGmailLabels",
  description: "List every Gmail account label available to the agent.",
  promptSnippet: "List every Gmail account label available to the agent.",
  parameters: list_gmail_labelsParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/list_gmail_labels",
        name: "list_gmail_labels",
      },
      params,
      signal,
    );
  },
};

export function registerListGmailLabelsTool(pi: ExtensionAPI): void {
  pi.registerTool(list_gmail_labelsTool);
}

export default function list_gmail_labelsToolExtension(pi: ExtensionAPI): void {
  registerListGmailLabelsTool(pi);
}
