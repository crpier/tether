// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const link_todo_memoryParameters = Type.Object({
  todo_id: Type.String({ format: "uuid" }),
  memory_id: Type.String({ format: "uuid" }),
});

export type LinkTodoMemoryParams = Static<typeof link_todo_memoryParameters>;

export const link_todo_memoryTool: ToolDefinition<
  typeof link_todo_memoryParameters,
  TetherToolDetails
> = {
  name: "link_todo_memory",
  label: "LinkTodoMemory",
  description: "Params for linking a Memory's context to a Todo.",
  promptSnippet: "Params for linking a Memory's context to a Todo.",
  parameters: link_todo_memoryParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/link_todo_memory",
        name: "link_todo_memory",
      },
      params,
      signal,
    );
  },
};

export function registerLinkTodoMemoryTool(pi: ExtensionAPI): void {
  pi.registerTool(link_todo_memoryTool);
}

export default function link_todo_memoryToolExtension(pi: ExtensionAPI): void {
  registerLinkTodoMemoryTool(pi);
}
