// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const captureParameters = Type.Object({
  content: Type.String({ minLength: 1 }),
  facets: Type.Optional(Type.Record(Type.String(), Type.String())),
});

export type CaptureParams = Static<typeof captureParameters>;

export const captureTool: ToolDefinition<
  typeof captureParameters,
  TetherToolDetails
> = {
  name: "capture",
  label: "Capture",
  description: "Params for capturing a loose Memory.",
  promptSnippet: "Params for capturing a loose Memory.",
  parameters: captureParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/capture", name: "capture" },
      params,
      signal,
    );
  },
};

export function registerCaptureTool(pi: ExtensionAPI): void {
  pi.registerTool(captureTool);
}

export default function captureToolExtension(pi: ExtensionAPI): void {
  registerCaptureTool(pi);
}
