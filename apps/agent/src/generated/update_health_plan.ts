// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const update_health_planParameters = Type.Object({
  exercise_types: Type.Array(
    StringEnum([
      "running",
      "strength_training",
      "walking",
      "weightlifting",
    ] as const),
  ),
  grace_minutes: Type.Optional(
    Type.Integer({ default: 60, maximum: 360, minimum: 15 }),
  ),
  timezone: Type.String({ minLength: 1 }),
  title: Type.String({ minLength: 1 }),
  windows: Type.Array(
    Type.Object({
      end_local_time: Type.String({ format: "time" }),
      start_local_time: Type.String({ format: "time" }),
      weekday: StringEnum([
        "monday",
        "tuesday",
        "wednesday",
        "thursday",
        "friday",
        "saturday",
        "sunday",
      ] as const),
    }),
  ),
  plan_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
});

export type UpdateHealthPlanParams = Static<
  typeof update_health_planParameters
>;

export const update_health_planTool: ToolDefinition<
  typeof update_health_planParameters,
  TetherToolDetails
> = {
  name: "update_health_plan",
  label: "UpdateHealthPlan",
  description:
    "Replace a complete Health plan definition at its observed version.",
  promptSnippet:
    "Replace a complete Health plan definition at its observed version.",
  parameters: update_health_planParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/update_health_plan",
        name: "update_health_plan",
      },
      params,
      signal,
    );
  },
};

export function registerUpdateHealthPlanTool(pi: ExtensionAPI): void {
  pi.registerTool(update_health_planTool);
}

export default function update_health_planToolExtension(
  pi: ExtensionAPI,
): void {
  registerUpdateHealthPlanTool(pi);
}
