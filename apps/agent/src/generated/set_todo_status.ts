// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const set_todo_statusParameters = Type.Object({
  todo_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
  status: StringEnum(["active", "completed", "abandoned"] as const),
});

export type SetTodoStatusParams = Static<typeof set_todo_statusParameters>;

export const set_todo_statusTool: ToolDefinition<
  typeof set_todo_statusParameters,
  TetherToolDetails
> = {
  name: "set_todo_status",
  label: "SetTodoStatus",
  description:
    "Params for transitioning a Todo's status at an observed version.",
  promptSnippet:
    "Params for transitioning a Todo's status at an observed version.",
  parameters: set_todo_statusParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/set_todo_status", name: "set_todo_status" },
      params,
      signal,
    );
  },
};

export function registerSetTodoStatusTool(pi: ExtensionAPI): void {
  pi.registerTool(set_todo_statusTool);
}

export default function set_todo_statusToolExtension(pi: ExtensionAPI): void {
  registerSetTodoStatusTool(pi);
}
