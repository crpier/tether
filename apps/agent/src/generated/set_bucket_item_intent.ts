// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const set_bucket_item_intentParameters = Type.Object({
  bucket_item_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
  intent_context: Type.String(),
});

export type SetBucketItemIntentParams = Static<
  typeof set_bucket_item_intentParameters
>;

export const set_bucket_item_intentTool: ToolDefinition<
  typeof set_bucket_item_intentParameters,
  TetherToolDetails
> = {
  name: "set_bucket_item_intent",
  label: "SetBucketItemIntent",
  description:
    "Params for attaching or replacing a Bucket item's intent context.\n\nThe one way to record a reason after Add — use it once the human supplies\none for an item that was Added without it.",
  promptSnippet:
    "Params for attaching or replacing a Bucket item's intent context.\n\nThe one way to record a reason after Add — use it once the human supplies\none for an item that was Added without it.",
  parameters: set_bucket_item_intentParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/set_bucket_item_intent",
        name: "set_bucket_item_intent",
      },
      params,
      signal,
    );
  },
};

export function registerSetBucketItemIntentTool(pi: ExtensionAPI): void {
  pi.registerTool(set_bucket_item_intentTool);
}

export default function set_bucket_item_intentToolExtension(
  pi: ExtensionAPI,
): void {
  registerSetBucketItemIntentTool(pi);
}
