// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const web_searchParameters = Type.Object({
  query: Type.String({ minLength: 1 }),
  max_results: Type.Optional(
    Type.Integer({ default: 5, maximum: 20, minimum: 1 }),
  ),
  search_depth: Type.Optional(StringEnum(["basic", "advanced"] as const)),
});

export type WebSearchParams = Static<typeof web_searchParameters>;

export const web_searchTool: ToolDefinition<
  typeof web_searchParameters,
  TetherToolDetails
> = {
  name: "web_search",
  label: "WebSearch",
  description:
    "Search the current public web and return snippets plus extracted content.",
  promptSnippet:
    "Search the current public web and return snippets plus extracted content.",
  parameters: web_searchParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/web_search", name: "web_search" },
      params,
      signal,
    );
  },
};

export function registerWebSearchTool(pi: ExtensionAPI): void {
  pi.registerTool(web_searchTool);
}

export default function web_searchToolExtension(pi: ExtensionAPI): void {
  registerWebSearchTool(pi);
}
