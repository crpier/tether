// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const browse_youtubeParameters = Type.Object({
  topic: Type.Optional(Type.String()),
  source: Type.Optional(StringEnum(["liked", "watch_later"] as const)),
});

export type BrowseYouTubeParams = Static<typeof browse_youtubeParameters>;

export const browse_youtubeTool: ToolDefinition<
  typeof browse_youtubeParameters,
  TetherToolDetails
> = {
  name: "browse_youtube",
  label: "BrowseYoutube",
  description: "Params for a topic/source-filtered browse of ingested videos.",
  promptSnippet:
    "Params for a topic/source-filtered browse of ingested videos.",
  parameters: browse_youtubeParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/browse_youtube", name: "browse_youtube" },
      params,
      signal,
    );
  },
};

export function registerBrowseYoutubeTool(pi: ExtensionAPI): void {
  pi.registerTool(browse_youtubeTool);
}

export default function browse_youtubeToolExtension(pi: ExtensionAPI): void {
  registerBrowseYoutubeTool(pi);
}
