// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const match_ebook_filenameParameters = Type.Object({
  filename: Type.String({ minLength: 1 }),
});

export type MatchEbookFilenameParams = Static<
  typeof match_ebook_filenameParameters
>;

export const match_ebook_filenameTool: ToolDefinition<
  typeof match_ebook_filenameParameters,
  TetherToolDetails
> = {
  name: "match_ebook_filename",
  label: "MatchEbookFilename",
  description: "Params for labeling the document a filename hashes to.",
  promptSnippet: "Params for labeling the document a filename hashes to.",
  parameters: match_ebook_filenameParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/match_ebook_filename",
        name: "match_ebook_filename",
      },
      params,
      signal,
    );
  },
};

export function registerMatchEbookFilenameTool(pi: ExtensionAPI): void {
  pi.registerTool(match_ebook_filenameTool);
}

export default function match_ebook_filenameToolExtension(
  pi: ExtensionAPI,
): void {
  registerMatchEbookFilenameTool(pi);
}
