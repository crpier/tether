// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const create_panelParameters = Type.Object({
  name: Type.String(),
  facets: Type.Record(Type.String(), Type.String()),
  query: Type.Optional(Type.String()),
  window_days: Type.Optional(Type.Integer({ exclusiveMinimum: 0 })),
  columns: Type.Optional(Type.Array(Type.String())),
  render_kind: Type.Optional(StringEnum(["table", "vega-lite"] as const)),
  vega_lite_spec: Type.Optional(Type.String()),
  position: Type.Optional(Type.Integer({ default: 0 })),
});

export type CreatePanelParams = Static<typeof create_panelParameters>;

export const create_panelTool: ToolDefinition<
  typeof create_panelParameters,
  TetherToolDetails
> = {
  name: "create_panel",
  label: "CreatePanel",
  description:
    "Params for saving a Synthetic panel.\n\nA panel must be scoped by `facets` and/or a text `query`; a `vega-lite`\nrender kind must carry its `vega_lite_spec` template. A malformed spec is\nrejected as a well-formed `invalid_input` envelope, never a corrupt row.",
  promptSnippet:
    "Params for saving a Synthetic panel.\n\nA panel must be scoped by `facets` and/or a text `query`; a `vega-lite`\nrender kind must carry its `vega_lite_spec` template. A malformed spec is\nrejected as a well-formed `invalid_input` envelope, never a corrupt row.",
  parameters: create_panelParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/create_panel", name: "create_panel" },
      params,
      signal,
    );
  },
};

export function registerCreatePanelTool(pi: ExtensionAPI): void {
  pi.registerTool(create_panelTool);
}

export default function create_panelToolExtension(pi: ExtensionAPI): void {
  registerCreatePanelTool(pi);
}
