// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const search_bucket_itemsParameters = Type.Object({
  q: Type.String(),
  limit: Type.Optional(Type.Integer({ default: 50, exclusiveMinimum: 0 })),
});

export type SearchBucketItemsParams = Static<
  typeof search_bucket_itemsParameters
>;

export const search_bucket_itemsTool: ToolDefinition<
  typeof search_bucket_itemsParameters,
  TetherToolDetails
> = {
  name: "search_bucket_items",
  label: "SearchBucketItems",
  description: "Params for keyword Search over active Bucket items.",
  promptSnippet: "Params for keyword Search over active Bucket items.",
  parameters: search_bucket_itemsParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/search_bucket_items",
        name: "search_bucket_items",
      },
      params,
      signal,
    );
  },
};

export function registerSearchBucketItemsTool(pi: ExtensionAPI): void {
  pi.registerTool(search_bucket_itemsTool);
}

export default function search_bucket_itemsToolExtension(
  pi: ExtensionAPI,
): void {
  registerSearchBucketItemsTool(pi);
}
