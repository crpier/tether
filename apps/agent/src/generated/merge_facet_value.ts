// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const merge_facet_valueParameters = Type.Object({
  key: Type.String(),
  old_value: Type.String(),
  new_value: Type.String(),
});

export type MergeFacetValueParams = Static<typeof merge_facet_valueParameters>;

export const merge_facet_valueTool: ToolDefinition<
  typeof merge_facet_valueParameters,
  TetherToolDetails
> = {
  name: "merge_facet_value",
  label: "MergeFacetValue",
  description:
    "Params for bulk-rewriting a Commons facet value across every Memory that carries it.\n\nDestructive to the old value across the whole corpus: the assistant must\nobtain the user's explicit approval in chat before calling this tool.",
  promptSnippet:
    "Params for bulk-rewriting a Commons facet value across every Memory that carries it.\n\nDestructive to the old value across the whole corpus: the assistant must\nobtain the user's explicit approval in chat before calling this tool.",
  parameters: merge_facet_valueParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/merge_facet_value",
        name: "merge_facet_value",
      },
      params,
      signal,
    );
  },
};

export function registerMergeFacetValueTool(pi: ExtensionAPI): void {
  pi.registerTool(merge_facet_valueTool);
}

export default function merge_facet_valueToolExtension(pi: ExtensionAPI): void {
  registerMergeFacetValueTool(pi);
}
