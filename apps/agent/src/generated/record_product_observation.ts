// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const record_product_observationParameters = Type.Object({
  interpretation: Type.String(),
});

export type RecordProductObservationParams = Static<
  typeof record_product_observationParameters
>;

export const record_product_observationTool: ToolDefinition<
  typeof record_product_observationParameters,
  TetherToolDetails
> = {
  name: "record_product_observation",
  label: "RecordProductObservation",
  description:
    "Interpretation of product feedback explicitly requested by the user.\n\nState the expected product behavior concisely. The host preserves the exact\nactive user Message and Conversation provenance without trusting copied text.\nNever call this tool merely because an interaction might be improved; the\nuser must explicitly ask to record product feedback.",
  promptSnippet:
    "Interpretation of product feedback explicitly requested by the user.\n\nState the expected product behavior concisely. The host preserves the exact\nactive user Message and Conversation provenance without trusting copied text.\nNever call this tool merely because an interaction might be improved; the\nuser must explicitly ask to record product feedback.",
  parameters: record_product_observationParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/record_product_observation",
        name: "record_product_observation",
      },
      params,
      signal,
    );
  },
};

export function registerRecordProductObservationTool(pi: ExtensionAPI): void {
  pi.registerTool(record_product_observationTool);
}

export default function record_product_observationToolExtension(
  pi: ExtensionAPI,
): void {
  registerRecordProductObservationTool(pi);
}
