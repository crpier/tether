// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const set_purchase_decisionParameters = Type.Object({
  bucket_item_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
  decision: StringEnum(["buy", "wait", "need-more-info"] as const),
});

export type SetPurchaseDecisionParams = Static<
  typeof set_purchase_decisionParameters
>;

export const set_purchase_decisionTool: ToolDefinition<
  typeof set_purchase_decisionParameters,
  TetherToolDetails
> = {
  name: "set_purchase_decision",
  label: "SetPurchaseDecision",
  description: "Params for recording the human's current purchase decision.",
  promptSnippet: "Params for recording the human's current purchase decision.",
  parameters: set_purchase_decisionParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/set_purchase_decision",
        name: "set_purchase_decision",
      },
      params,
      signal,
    );
  },
};

export function registerSetPurchaseDecisionTool(pi: ExtensionAPI): void {
  pi.registerTool(set_purchase_decisionTool);
}

export default function set_purchase_decisionToolExtension(
  pi: ExtensionAPI,
): void {
  registerSetPurchaseDecisionTool(pi);
}
