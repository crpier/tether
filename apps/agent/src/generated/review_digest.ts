// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const review_digestParameters = Type.Object({});

export type ReviewDigestParams = Static<typeof review_digestParameters>;

export const review_digestTool: ToolDefinition<
  typeof review_digestParameters,
  TetherToolDetails
> = {
  name: "review_digest",
  label: "ReviewDigest",
  description:
    "Params for the AI-assisted Review digest.\n\nThe digest is computed over the whole live queue, so it takes no inputs\nbeyond the session identity the gate already requires.",
  promptSnippet:
    "Params for the AI-assisted Review digest.\n\nThe digest is computed over the whole live queue, so it takes no inputs\nbeyond the session identity the gate already requires.",
  parameters: review_digestParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/review_digest", name: "review_digest" },
      params,
      signal,
    );
  },
};

export function registerReviewDigestTool(pi: ExtensionAPI): void {
  pi.registerTool(review_digestTool);
}

export default function review_digestToolExtension(pi: ExtensionAPI): void {
  registerReviewDigestTool(pi);
}
