// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_unlabeled_ebooksParameters = Type.Object({});

export type ListUnlabeledEbooksParams = Static<
  typeof list_unlabeled_ebooksParameters
>;

export const list_unlabeled_ebooksTool: ToolDefinition<
  typeof list_unlabeled_ebooksParameters,
  TetherToolDetails
> = {
  name: "list_unlabeled_ebooks",
  label: "ListUnlabeledEbooks",
  description:
    "Params for listing documents still without a title (takes none).",
  promptSnippet:
    "Params for listing documents still without a title (takes none).",
  parameters: list_unlabeled_ebooksParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/list_unlabeled_ebooks",
        name: "list_unlabeled_ebooks",
      },
      params,
      signal,
    );
  },
};

export function registerListUnlabeledEbooksTool(pi: ExtensionAPI): void {
  pi.registerTool(list_unlabeled_ebooksTool);
}

export default function list_unlabeled_ebooksToolExtension(
  pi: ExtensionAPI,
): void {
  registerListUnlabeledEbooksTool(pi);
}
