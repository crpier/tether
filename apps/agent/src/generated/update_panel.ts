// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const update_panelParameters = Type.Object({
  name: Type.String(),
  facets: Type.Record(Type.String(), Type.String()),
  query: Type.Optional(Type.String()),
  window_days: Type.Optional(Type.Integer({ exclusiveMinimum: 0 })),
  columns: Type.Optional(Type.Array(Type.String())),
  render_kind: Type.Optional(StringEnum(["table", "vega-lite"] as const)),
  vega_lite_spec: Type.Optional(Type.String()),
  position: Type.Optional(Type.Integer({ default: 0 })),
  panel_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
});

export type UpdatePanelParams = Static<typeof update_panelParameters>;

export const update_panelTool: ToolDefinition<
  typeof update_panelParameters,
  TetherToolDetails
> = {
  name: "update_panel",
  label: "UpdatePanel",
  description:
    "Params for replacing a panel's definition at an observed version.",
  promptSnippet:
    "Params for replacing a panel's definition at an observed version.",
  parameters: update_panelParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/update_panel", name: "update_panel" },
      params,
      signal,
    );
  },
};

export function registerUpdatePanelTool(pi: ExtensionAPI): void {
  pi.registerTool(update_panelTool);
}

export default function update_panelToolExtension(pi: ExtensionAPI): void {
  registerUpdatePanelTool(pi);
}
