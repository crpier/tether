// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const propose_essay_gradeParameters = Type.Object({
  prompt_id: Type.String({ format: "uuid" }),
  answer_text: Type.String(),
});

export type ProposeEssayGradeParams = Static<
  typeof propose_essay_gradeParameters
>;

export const propose_essay_gradeTool: ToolDefinition<
  typeof propose_essay_gradeParameters,
  TetherToolDetails
> = {
  name: "propose_essay_grade",
  label: "ProposeEssayGrade",
  description:
    "Params for proposing an essay grade against the rubric.\n\nThe proposal is advisory: the human confirms or overrides it before the\nanswer is submitted (ADR 0004), through the web surface's answer route.",
  promptSnippet:
    "Params for proposing an essay grade against the rubric.\n\nThe proposal is advisory: the human confirms or overrides it before the\nanswer is submitted (ADR 0004), through the web surface's answer route.",
  parameters: propose_essay_gradeParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/propose_essay_grade",
        name: "propose_essay_grade",
      },
      params,
      signal,
    );
  },
};

export function registerProposeEssayGradeTool(pi: ExtensionAPI): void {
  pi.registerTool(propose_essay_gradeTool);
}

export default function propose_essay_gradeToolExtension(
  pi: ExtensionAPI,
): void {
  registerProposeEssayGradeTool(pi);
}
