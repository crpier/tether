// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const start_recallParameters = Type.Object({
  video_id: Type.String(),
});

export type StartRecallParams = Static<typeof start_recallParameters>;

export const start_recallTool: ToolDefinition<
  typeof start_recallParameters,
  TetherToolDetails
> = {
  name: "start_recall",
  label: "StartRecall",
  description:
    "Params for promoting an ingested educational video into a study item.",
  promptSnippet:
    "Params for promoting an ingested educational video into a study item.",
  parameters: start_recallParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/start_recall", name: "start_recall" },
      params,
      signal,
    );
  },
};

export function registerStartRecallTool(pi: ExtensionAPI): void {
  pi.registerTool(start_recallTool);
}

export default function start_recallToolExtension(pi: ExtensionAPI): void {
  registerStartRecallTool(pi);
}
