// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_panelsParameters = Type.Object({});

export type ListPanelsParams = Static<typeof list_panelsParameters>;

export const list_panelsTool: ToolDefinition<
  typeof list_panelsParameters,
  TetherToolDetails
> = {
  name: "list_panels",
  label: "ListPanels",
  description: "Params for listing live panels, in position order.",
  promptSnippet: "Params for listing live panels, in position order.",
  parameters: list_panelsParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/list_panels", name: "list_panels" },
      params,
      signal,
    );
  },
};

export function registerListPanelsTool(pi: ExtensionAPI): void {
  pi.registerTool(list_panelsTool);
}

export default function list_panelsToolExtension(pi: ExtensionAPI): void {
  registerListPanelsTool(pi);
}
