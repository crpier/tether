// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const browseParameters = Type.Object({
  state: StringEnum(["loose", "tethered"] as const),
});

export type BrowseParams = Static<typeof browseParameters>;

export const browseTool: ToolDefinition<
  typeof browseParameters,
  TetherToolDetails
> = {
  name: "browse",
  label: "Browse",
  description:
    "Params for the review queue (`loose`) / corpus browse (`tethered`).",
  promptSnippet:
    "Params for the review queue (`loose`) / corpus browse (`tethered`).",
  parameters: browseParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/browse", name: "browse" },
      params,
      signal,
    );
  },
};

export function registerBrowseTool(pi: ExtensionAPI): void {
  pi.registerTool(browseTool);
}

export default function browseToolExtension(pi: ExtensionAPI): void {
  registerBrowseTool(pi);
}
