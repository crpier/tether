// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const triage_reportParameters = Type.Object({});

export type TriageReportParams = Static<typeof triage_reportParameters>;

export const triage_reportTool: ToolDefinition<
  typeof triage_reportParameters,
  TetherToolDetails
> = {
  name: "triage_report",
  label: "TriageReport",
  description:
    "Params for the Triage report.\n\nThe report is computed over the whole active Bucket list, so it takes no\ninputs beyond the session identity the gate already requires.",
  promptSnippet:
    "Params for the Triage report.\n\nThe report is computed over the whole active Bucket list, so it takes no\ninputs beyond the session identity the gate already requires.",
  parameters: triage_reportParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/triage_report", name: "triage_report" },
      params,
      signal,
    );
  },
};

export function registerTriageReportTool(pi: ExtensionAPI): void {
  pi.registerTool(triage_reportTool);
}

export default function triage_reportToolExtension(pi: ExtensionAPI): void {
  registerTriageReportTool(pi);
}
