// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const trash_gmail_messageParameters = Type.Object({
  message_id: Type.String({ minLength: 1 }),
});

export type TrashGmailMessageParams = Static<
  typeof trash_gmail_messageParameters
>;

export const trash_gmail_messageTool: ToolDefinition<
  typeof trash_gmail_messageParameters,
  TetherToolDetails
> = {
  name: "trash_gmail_message",
  label: "TrashGmailMessage",
  description:
    "Move one Gmail message to Trash without permanently deleting it.",
  promptSnippet:
    "Move one Gmail message to Trash without permanently deleting it.",
  parameters: trash_gmail_messageParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/trash_gmail_message",
        name: "trash_gmail_message",
      },
      params,
      signal,
    );
  },
};

export function registerTrashGmailMessageTool(pi: ExtensionAPI): void {
  pi.registerTool(trash_gmail_messageTool);
}

export default function trash_gmail_messageToolExtension(
  pi: ExtensionAPI,
): void {
  registerTrashGmailMessageTool(pi);
}
