// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const delete_bucket_itemParameters = Type.Object({
  bucket_item_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
});

export type DeleteBucketItemParams = Static<
  typeof delete_bucket_itemParameters
>;

export const delete_bucket_itemTool: ToolDefinition<
  typeof delete_bucket_itemParameters,
  TetherToolDetails
> = {
  name: "delete_bucket_item",
  label: "DeleteBucketItem",
  description: "Params for deleting a Bucket item at an observed version.",
  promptSnippet: "Params for deleting a Bucket item at an observed version.",
  parameters: delete_bucket_itemParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/delete_bucket_item",
        name: "delete_bucket_item",
      },
      params,
      signal,
    );
  },
};

export function registerDeleteBucketItemTool(pi: ExtensionAPI): void {
  pi.registerTool(delete_bucket_itemTool);
}

export default function delete_bucket_itemToolExtension(
  pi: ExtensionAPI,
): void {
  registerDeleteBucketItemTool(pi);
}
