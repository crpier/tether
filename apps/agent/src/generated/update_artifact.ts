// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const update_artifactParameters = Type.Object({
  id: Type.String({ format: "uuid" }),
  html: Type.String(),
});

export type UpdateArtifactParams = Static<typeof update_artifactParameters>;

export const update_artifactTool: ToolDefinition<
  typeof update_artifactParameters,
  TetherToolDetails
> = {
  name: "update_artifact",
  label: "UpdateArtifact",
  description:
    "Params for appending a new version onto an existing artifact.\n\n`id` names the stable artifact identity (not a specific version's own row\nid); the title carries forward from the latest version unchanged.",
  promptSnippet:
    "Params for appending a new version onto an existing artifact.\n\n`id` names the stable artifact identity (not a specific version's own row\nid); the title carries forward from the latest version unchanged.",
  parameters: update_artifactParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/update_artifact", name: "update_artifact" },
      params,
      signal,
    );
  },
};

export function registerUpdateArtifactTool(pi: ExtensionAPI): void {
  pi.registerTool(update_artifactTool);
}

export default function update_artifactToolExtension(pi: ExtensionAPI): void {
  registerUpdateArtifactTool(pi);
}
