// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const facet_overviewParameters = Type.Object({});

export type FacetOverviewParams = Static<typeof facet_overviewParameters>;

export const facet_overviewTool: ToolDefinition<
  typeof facet_overviewParameters,
  TetherToolDetails
> = {
  name: "facet_overview",
  label: "FacetOverview",
  description:
    "Params for the Commons facet overview: distinct keys/values with counts.\n\nRead-only; takes no inputs beyond the session identity the gate already\nrequires.",
  promptSnippet:
    "Params for the Commons facet overview: distinct keys/values with counts.\n\nRead-only; takes no inputs beyond the session identity the gate already\nrequires.",
  parameters: facet_overviewParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/facet_overview", name: "facet_overview" },
      params,
      signal,
    );
  },
};

export function registerFacetOverviewTool(pi: ExtensionAPI): void {
  pi.registerTool(facet_overviewTool);
}

export default function facet_overviewToolExtension(pi: ExtensionAPI): void {
  registerFacetOverviewTool(pi);
}
