// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const delete_panelParameters = Type.Object({
  panel_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
});

export type DeletePanelParams = Static<typeof delete_panelParameters>;

export const delete_panelTool: ToolDefinition<
  typeof delete_panelParameters,
  TetherToolDetails
> = {
  name: "delete_panel",
  label: "DeletePanel",
  description: "Params for deleting a panel at an observed version.",
  promptSnippet: "Params for deleting a panel at an observed version.",
  parameters: delete_panelParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/delete_panel", name: "delete_panel" },
      params,
      signal,
    );
  },
};

export function registerDeletePanelTool(pi: ExtensionAPI): void {
  pi.registerTool(delete_panelTool);
}

export default function delete_panelToolExtension(pi: ExtensionAPI): void {
  registerDeletePanelTool(pi);
}
