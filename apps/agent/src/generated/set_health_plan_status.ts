// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const set_health_plan_statusParameters = Type.Object({
  plan_id: Type.String({ format: "uuid" }),
  status: StringEnum(["active", "paused"] as const),
  version: Type.Integer({ exclusiveMinimum: 0 }),
});

export type SetHealthPlanStatusParams = Static<
  typeof set_health_plan_statusParameters
>;

export const set_health_plan_statusTool: ToolDefinition<
  typeof set_health_plan_statusParameters,
  TetherToolDetails
> = {
  name: "set_health_plan_status",
  label: "SetHealthPlanStatus",
  description: "Pause or resume a Health plan at its observed version.",
  promptSnippet: "Pause or resume a Health plan at its observed version.",
  parameters: set_health_plan_statusParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/set_health_plan_status",
        name: "set_health_plan_status",
      },
      params,
      signal,
    );
  },
};

export function registerSetHealthPlanStatusTool(pi: ExtensionAPI): void {
  pi.registerTool(set_health_plan_statusTool);
}

export default function set_health_plan_statusToolExtension(
  pi: ExtensionAPI,
): void {
  registerSetHealthPlanStatusTool(pi);
}
