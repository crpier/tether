// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const tetherParameters = Type.Object({
  memory_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
});

export type TetherParams = Static<typeof tetherParameters>;

export const tetherTool: ToolDefinition<
  typeof tetherParameters,
  TetherToolDetails
> = {
  name: "tether",
  label: "Tether",
  description: "Params for promoting a loose Memory to tethered.",
  promptSnippet: "Params for promoting a loose Memory to tethered.",
  parameters: tetherParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/tether", name: "tether" },
      params,
      signal,
    );
  },
};

export function registerTetherTool(pi: ExtensionAPI): void {
  pi.registerTool(tetherTool);
}

export default function tetherToolExtension(pi: ExtensionAPI): void {
  registerTetherTool(pi);
}
