// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const archive_gmail_messageParameters = Type.Object({
  message_id: Type.String({ minLength: 1 }),
});

export type ArchiveGmailMessageParams = Static<
  typeof archive_gmail_messageParameters
>;

export const archive_gmail_messageTool: ToolDefinition<
  typeof archive_gmail_messageParameters,
  TetherToolDetails
> = {
  name: "archive_gmail_message",
  label: "ArchiveGmailMessage",
  description: "Archive one Gmail message by removing its inbox label.",
  promptSnippet: "Archive one Gmail message by removing its inbox label.",
  parameters: archive_gmail_messageParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/archive_gmail_message",
        name: "archive_gmail_message",
      },
      params,
      signal,
    );
  },
};

export function registerArchiveGmailMessageTool(pi: ExtensionAPI): void {
  pi.registerTool(archive_gmail_messageTool);
}

export default function archive_gmail_messageToolExtension(
  pi: ExtensionAPI,
): void {
  registerArchiveGmailMessageTool(pi);
}
