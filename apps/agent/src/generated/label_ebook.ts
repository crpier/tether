// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const label_ebookParameters = Type.Object({
  document_hash: Type.String({ minLength: 1 }),
  title: Type.String({ minLength: 1 }),
});

export type LabelEbookParams = Static<typeof label_ebookParameters>;

export const label_ebookTool: ToolDefinition<
  typeof label_ebookParameters,
  TetherToolDetails
> = {
  name: "label_ebook",
  label: "LabelEbook",
  description: "Params for attaching a human title to a document hash.",
  promptSnippet: "Params for attaching a human title to a document hash.",
  parameters: label_ebookParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/label_ebook", name: "label_ebook" },
      params,
      signal,
    );
  },
};

export function registerLabelEbookTool(pi: ExtensionAPI): void {
  pi.registerTool(label_ebookTool);
}

export default function label_ebookToolExtension(pi: ExtensionAPI): void {
  registerLabelEbookTool(pi);
}
