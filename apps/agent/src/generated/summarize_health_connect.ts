// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const summarize_health_connectParameters = Type.Object({
  after: Type.String({ format: "date-time" }),
  before: Type.String({ format: "date-time" }),
  bucket: Type.Optional(StringEnum(["none", "day"] as const)),
});

export type SummarizeHealthConnectParams = Static<
  typeof summarize_health_connectParameters
>;

export const summarize_health_connectTool: ToolDefinition<
  typeof summarize_health_connectParameters,
  TetherToolDetails
> = {
  name: "summarize_health_connect",
  label: "SummarizeHealthConnect",
  description:
    "Use compact Health Connect aggregates for overview and trend requests.",
  promptSnippet:
    "Use compact Health Connect aggregates for overview and trend requests.",
  parameters: summarize_health_connectParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/summarize_health_connect",
        name: "summarize_health_connect",
      },
      params,
      signal,
    );
  },
};

export function registerSummarizeHealthConnectTool(pi: ExtensionAPI): void {
  pi.registerTool(summarize_health_connectTool);
}

export default function summarize_health_connectToolExtension(
  pi: ExtensionAPI,
): void {
  registerSummarizeHealthConnectTool(pi);
}
