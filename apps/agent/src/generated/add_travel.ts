// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const add_travelParameters = Type.Object({
  destination: Type.String(),
  intent_context: Type.String(),
  season: Type.Optional(Type.String()),
});

export type AddTravelParams = Static<typeof add_travelParameters>;

export const add_travelTool: ToolDefinition<
  typeof add_travelParameters,
  TetherToolDetails
> = {
  name: "add_travel",
  label: "AddTravel",
  description: "Params for Adding a `travel` Bucket item.",
  promptSnippet: "Params for Adding a `travel` Bucket item.",
  parameters: add_travelParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/add_travel", name: "add_travel" },
      params,
      signal,
    );
  },
};

export function registerAddTravelTool(pi: ExtensionAPI): void {
  pi.registerTool(add_travelTool);
}

export default function add_travelToolExtension(pi: ExtensionAPI): void {
  registerAddTravelTool(pi);
}
