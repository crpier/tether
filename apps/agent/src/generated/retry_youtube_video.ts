// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const retry_youtube_videoParameters = Type.Object({
  video_id: Type.String(),
});

export type RetryYouTubeVideoParams = Static<
  typeof retry_youtube_videoParameters
>;

export const retry_youtube_videoTool: ToolDefinition<
  typeof retry_youtube_videoParameters,
  TetherToolDetails
> = {
  name: "retry_youtube_video",
  label: "RetryYoutubeVideo",
  description: "Params for returning a previously purged video to ingestion.",
  promptSnippet: "Params for returning a previously purged video to ingestion.",
  parameters: retry_youtube_videoParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/retry_youtube_video",
        name: "retry_youtube_video",
      },
      params,
      signal,
    );
  },
};

export function registerRetryYoutubeVideoTool(pi: ExtensionAPI): void {
  pi.registerTool(retry_youtube_videoTool);
}

export default function retry_youtube_videoToolExtension(
  pi: ExtensionAPI,
): void {
  registerRetryYoutubeVideoTool(pi);
}
