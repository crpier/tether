// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const add_movieParameters = Type.Object({
  title: Type.String(),
  intent_context: Type.String(),
  year: Type.Optional(Type.Integer()),
});

export type AddMovieParams = Static<typeof add_movieParameters>;

export const add_movieTool: ToolDefinition<
  typeof add_movieParameters,
  TetherToolDetails
> = {
  name: "add_movie",
  label: "AddMovie",
  description: "Params for Adding a `movie` Bucket item.",
  promptSnippet: "Params for Adding a `movie` Bucket item.",
  parameters: add_movieParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/add_movie", name: "add_movie" },
      params,
      signal,
    );
  },
};

export function registerAddMovieTool(pi: ExtensionAPI): void {
  pi.registerTool(add_movieTool);
}

export default function add_movieToolExtension(pi: ExtensionAPI): void {
  registerAddMovieTool(pi);
}
