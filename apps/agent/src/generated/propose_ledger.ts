// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const propose_ledgerParameters = Type.Object({
  fields: Type.Array(
    Type.Object({
      deprecated: Type.Optional(Type.Boolean({ default: false })),
      description: Type.String({ minLength: 1 }),
      enum_values: Type.Optional(
        Type.Array(Type.String(), { maxItems: 64, minItems: 1 }),
      ),
      field_id: Type.String({ minLength: 1 }),
      label: Type.String({ minLength: 1 }),
      required: Type.Boolean(),
      type: StringEnum([
        "boolean",
        "date",
        "datetime",
        "decimal",
        "enum",
        "integer",
        "text",
      ] as const),
      unit: Type.Optional(Type.String({ minLength: 1 })),
    }),
    { maxItems: 32, minItems: 1 },
  ),
  name: Type.String({ minLength: 1 }),
  purpose: Type.String({ minLength: 1 }),
  status: Type.Optional(
    StringEnum(["active", "completed", "abandoned"] as const),
  ),
});

export type ProposeLedgerParams = Static<typeof propose_ledgerParameters>;

export const propose_ledgerTool: ToolDefinition<
  typeof propose_ledgerParameters,
  TetherToolDetails
> = {
  name: "propose_ledger",
  label: "ProposeLedger",
  description:
    "Propose a flat structured history for later explicit user approval.\n\nUse only when no established Vertical owns the records. This freezes a\nproposal but creates no writable Ledger. A later interactive user Message\nmust approve the exact proposal.",
  promptSnippet:
    "Propose a flat structured history for later explicit user approval.\n\nUse only when no established Vertical owns the records. This freezes a\nproposal but creates no writable Ledger. A later interactive user Message\nmust approve the exact proposal.",
  parameters: propose_ledgerParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/propose_ledger", name: "propose_ledger" },
      params,
      signal,
    );
  },
};

export function registerProposeLedgerTool(pi: ExtensionAPI): void {
  pi.registerTool(propose_ledgerTool);
}

export default function propose_ledgerToolExtension(pi: ExtensionAPI): void {
  registerProposeLedgerTool(pi);
}
