// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const health_connect_inventoryParameters = Type.Object({});

export type HealthConnectInventoryParams = Static<
  typeof health_connect_inventoryParameters
>;

export const health_connect_inventoryTool: ToolDefinition<
  typeof health_connect_inventoryParameters,
  TetherToolDetails
> = {
  name: "health_connect_inventory",
  label: "HealthConnectInventory",
  description:
    "List populated Health Connect record types and their UTC time bounds.",
  promptSnippet:
    "List populated Health Connect record types and their UTC time bounds.",
  parameters: health_connect_inventoryParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/health_connect_inventory",
        name: "health_connect_inventory",
      },
      params,
      signal,
    );
  },
};

export function registerHealthConnectInventoryTool(pi: ExtensionAPI): void {
  pi.registerTool(health_connect_inventoryTool);
}

export default function health_connect_inventoryToolExtension(
  pi: ExtensionAPI,
): void {
  registerHealthConnectInventoryTool(pi);
}
