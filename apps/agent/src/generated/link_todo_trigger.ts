// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const link_todo_triggerParameters = Type.Object({
  todo_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
  trigger_id: Type.String({ format: "uuid" }),
});

export type LinkTodoTriggerParams = Static<typeof link_todo_triggerParameters>;

export const link_todo_triggerTool: ToolDefinition<
  typeof link_todo_triggerParameters,
  TetherToolDetails
> = {
  name: "link_todo_trigger",
  label: "LinkTodoTrigger",
  description:
    "Params for attaching a scheduled trigger (a deadline) to a Todo.",
  promptSnippet:
    "Params for attaching a scheduled trigger (a deadline) to a Todo.",
  parameters: link_todo_triggerParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/link_todo_trigger",
        name: "link_todo_trigger",
      },
      params,
      signal,
    );
  },
};

export function registerLinkTodoTriggerTool(pi: ExtensionAPI): void {
  pi.registerTool(link_todo_triggerTool);
}

export default function link_todo_triggerToolExtension(pi: ExtensionAPI): void {
  registerLinkTodoTriggerTool(pi);
}
