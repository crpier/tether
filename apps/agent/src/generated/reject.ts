// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const rejectParameters = Type.Object({
  memory_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
});

export type RejectParams = Static<typeof rejectParameters>;

export const rejectTool: ToolDefinition<
  typeof rejectParameters,
  TetherToolDetails
> = {
  name: "reject",
  label: "Reject",
  description:
    "Params for soft-deleting (rejecting) a Memory at an observed version.",
  promptSnippet:
    "Params for soft-deleting (rejecting) a Memory at an observed version.",
  parameters: rejectParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/reject", name: "reject" },
      params,
      signal,
    );
  },
};

export function registerRejectTool(pi: ExtensionAPI): void {
  pi.registerTool(rejectTool);
}

export default function rejectToolExtension(pi: ExtensionAPI): void {
  registerRejectTool(pi);
}
