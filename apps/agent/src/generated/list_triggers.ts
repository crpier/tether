// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_triggersParameters = Type.Object({});

export type ListTriggersParams = Static<typeof list_triggersParameters>;

export const list_triggersTool: ToolDefinition<
  typeof list_triggersParameters,
  TetherToolDetails
> = {
  name: "list_triggers",
  label: "ListTriggers",
  description: "Params for listing live triggers; the listing takes no inputs.",
  promptSnippet:
    "Params for listing live triggers; the listing takes no inputs.",
  parameters: list_triggersParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/list_triggers", name: "list_triggers" },
      params,
      signal,
    );
  },
};

export function registerListTriggersTool(pi: ExtensionAPI): void {
  pi.registerTool(list_triggersTool);
}

export default function list_triggersToolExtension(pi: ExtensionAPI): void {
  registerListTriggersTool(pi);
}
