// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const append_ledger_entriesParameters = Type.Object({
  entries: Type.Array(
    Type.Object({
      occurred_at: Type.Optional(Type.String({ format: "date-time" })),
      supersedes_entry_id: Type.Optional(Type.String({ format: "uuid" })),
      values: Type.Record(
        Type.String(),
        Type.Union([Type.Boolean(), Type.Integer(), Type.String()]),
        { maxProperties: 32, minProperties: 1 },
      ),
    }),
    { maxItems: 25, minItems: 1 },
  ),
  ledger_id: Type.String({ format: "uuid" }),
  revision: Type.Integer({ exclusiveMinimum: 0 }),
});

export type AppendLedgerEntriesParams = Static<
  typeof append_ledger_entriesParameters
>;

export const append_ledger_entriesTool: ToolDefinition<
  typeof append_ledger_entriesParameters,
  TetherToolDetails
> = {
  name: "append_ledger_entries",
  label: "AppendLedgerEntries",
  description:
    "Append an atomic bounded batch under one observed active schema.\n\nValues must satisfy the approved field definitions. Every appended record\ncites the active user Message automatically. Set `supersedes_entry_id` only\nfor a complete correction of one current entry.",
  promptSnippet:
    "Append an atomic bounded batch under one observed active schema.\n\nValues must satisfy the approved field definitions. Every appended record\ncites the active user Message automatically. Set `supersedes_entry_id` only\nfor a complete correction of one current entry.",
  parameters: append_ledger_entriesParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/append_ledger_entries",
        name: "append_ledger_entries",
      },
      params,
      signal,
    );
  },
};

export function registerAppendLedgerEntriesTool(pi: ExtensionAPI): void {
  pi.registerTool(append_ledger_entriesTool);
}

export default function append_ledger_entriesToolExtension(
  pi: ExtensionAPI,
): void {
  registerAppendLedgerEntriesTool(pi);
}
