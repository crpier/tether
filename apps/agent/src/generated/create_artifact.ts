// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const create_artifactParameters = Type.Object({
  title: Type.String(),
  html: Type.String(),
});

export type CreateArtifactParams = Static<typeof create_artifactParameters>;

export const create_artifactTool: ToolDefinition<
  typeof create_artifactParameters,
  TetherToolDetails
> = {
  name: "create_artifact",
  label: "CreateArtifact",
  description: "Params for creating a new artifact at version 1.",
  promptSnippet: "Params for creating a new artifact at version 1.",
  parameters: create_artifactParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/create_artifact", name: "create_artifact" },
      params,
      signal,
    );
  },
};

export function registerCreateArtifactTool(pi: ExtensionAPI): void {
  pi.registerTool(create_artifactTool);
}

export default function create_artifactToolExtension(pi: ExtensionAPI): void {
  registerCreateArtifactTool(pi);
}
