// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const rename_facet_keyParameters = Type.Object({
  old_key: Type.String(),
  new_key: Type.String(),
});

export type RenameFacetKeyParams = Static<typeof rename_facet_keyParameters>;

export const rename_facet_keyTool: ToolDefinition<
  typeof rename_facet_keyParameters,
  TetherToolDetails
> = {
  name: "rename_facet_key",
  label: "RenameFacetKey",
  description:
    "Params for bulk-renaming a Commons facet key across every Memory that carries it.\n\nDestructive to the old key name across the whole corpus: the assistant\nmust obtain the user's explicit approval in chat before calling this tool.",
  promptSnippet:
    "Params for bulk-renaming a Commons facet key across every Memory that carries it.\n\nDestructive to the old key name across the whole corpus: the assistant\nmust obtain the user's explicit approval in chat before calling this tool.",
  parameters: rename_facet_keyParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/rename_facet_key",
        name: "rename_facet_key",
      },
      params,
      signal,
    );
  },
};

export function registerRenameFacetKeyTool(pi: ExtensionAPI): void {
  pi.registerTool(rename_facet_keyTool);
}

export default function rename_facet_keyToolExtension(pi: ExtensionAPI): void {
  registerRenameFacetKeyTool(pi);
}
