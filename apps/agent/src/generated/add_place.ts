// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const add_placeParameters = Type.Object({
  name: Type.String(),
  intent_context: Type.Optional(Type.String()),
  location: Type.Optional(Type.String()),
});

export type AddPlaceParams = Static<typeof add_placeParameters>;

export const add_placeTool: ToolDefinition<
  typeof add_placeParameters,
  TetherToolDetails
> = {
  name: "add_place",
  label: "AddPlace",
  description:
    "Params for Adding a `place` Bucket item.\n\n`intent_context` is optional — add the item now even without one; a reason\ncan be attached later with `set_bucket_item_intent`.",
  promptSnippet:
    "Params for Adding a `place` Bucket item.\n\n`intent_context` is optional — add the item now even without one; a reason\ncan be attached later with `set_bucket_item_intent`.",
  parameters: add_placeParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/add_place", name: "add_place" },
      params,
      signal,
    );
  },
};

export function registerAddPlaceTool(pi: ExtensionAPI): void {
  pi.registerTool(add_placeTool);
}

export default function add_placeToolExtension(pi: ExtensionAPI): void {
  registerAddPlaceTool(pi);
}
