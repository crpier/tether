// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const approve_ledger_proposalParameters = Type.Object({
  proposal_id: Type.String({ format: "uuid" }),
});

export type ApproveLedgerProposalParams = Static<
  typeof approve_ledger_proposalParameters
>;

export const approve_ledger_proposalTool: ToolDefinition<
  typeof approve_ledger_proposalParameters,
  TetherToolDetails
> = {
  name: "approve_ledger_proposal",
  label: "ApproveLedgerProposal",
  description:
    "Approve one exact proposal after the user explicitly confirms it.",
  promptSnippet:
    "Approve one exact proposal after the user explicitly confirms it.",
  parameters: approve_ledger_proposalParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/approve_ledger_proposal",
        name: "approve_ledger_proposal",
      },
      params,
      signal,
    );
  },
};

export function registerApproveLedgerProposalTool(pi: ExtensionAPI): void {
  pi.registerTool(approve_ledger_proposalTool);
}

export default function approve_ledger_proposalToolExtension(
  pi: ExtensionAPI,
): void {
  registerApproveLedgerProposalTool(pi);
}
