// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const search_gmailParameters = Type.Object({
  query: Type.String({ minLength: 1 }),
  max_results: Type.Optional(
    Type.Integer({ default: 20, maximum: 50, minimum: 1 }),
  ),
  page_token: Type.Optional(Type.String()),
});

export type GmailSearchParams = Static<typeof search_gmailParameters>;

export const search_gmailTool: ToolDefinition<
  typeof search_gmailParameters,
  TetherToolDetails
> = {
  name: "search_gmail",
  label: "SearchGmail",
  description: "Search Gmail message metadata by query term, paged by token.",
  promptSnippet: "Search Gmail message metadata by query term, paged by token.",
  parameters: search_gmailParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/search_gmail", name: "search_gmail" },
      params,
      signal,
    );
  },
};

export function registerSearchGmailTool(pi: ExtensionAPI): void {
  pi.registerTool(search_gmailTool);
}

export default function search_gmailToolExtension(pi: ExtensionAPI): void {
  registerSearchGmailTool(pi);
}
