// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const query_ledger_entriesParameters = Type.Object({
  after: Type.Optional(Type.String({ format: "date-time" })),
  before: Type.Optional(Type.String({ format: "date-time" })),
  field_equals: Type.Optional(
    Type.Record(
      Type.String(),
      Type.Union([Type.Boolean(), Type.Integer(), Type.String()]),
    ),
  ),
  include_superseded: Type.Optional(Type.Boolean({ default: false })),
  ledger_id: Type.Optional(Type.String({ format: "uuid" })),
  limit: Type.Optional(Type.Integer({ default: 50, maximum: 100, minimum: 1 })),
  q: Type.Optional(Type.String({ default: "" })),
});

export type QueryLedgerEntriesParams = Static<
  typeof query_ledger_entriesParameters
>;

export const query_ledger_entriesTool: ToolDefinition<
  typeof query_ledger_entriesParameters,
  TetherToolDetails
> = {
  name: "query_ledger_entries",
  label: "QueryLedgerEntries",
  description:
    "Query bounded generic records separately from current Memory Search.",
  promptSnippet:
    "Query bounded generic records separately from current Memory Search.",
  parameters: query_ledger_entriesParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/query_ledger_entries",
        name: "query_ledger_entries",
      },
      params,
      signal,
    );
  },
};

export function registerQueryLedgerEntriesTool(pi: ExtensionAPI): void {
  pi.registerTool(query_ledger_entriesTool);
}

export default function query_ledger_entriesToolExtension(
  pi: ExtensionAPI,
): void {
  registerQueryLedgerEntriesTool(pi);
}
