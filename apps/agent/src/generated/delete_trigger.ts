// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const delete_triggerParameters = Type.Object({
  trigger_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
});

export type DeleteTriggerParams = Static<typeof delete_triggerParameters>;

export const delete_triggerTool: ToolDefinition<
  typeof delete_triggerParameters,
  TetherToolDetails
> = {
  name: "delete_trigger",
  label: "DeleteTrigger",
  description: "Params for deleting a trigger at an observed version.",
  promptSnippet: "Params for deleting a trigger at an observed version.",
  parameters: delete_triggerParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/delete_trigger", name: "delete_trigger" },
      params,
      signal,
    );
  },
};

export function registerDeleteTriggerTool(pi: ExtensionAPI): void {
  pi.registerTool(delete_triggerTool);
}

export default function delete_triggerToolExtension(pi: ExtensionAPI): void {
  registerDeleteTriggerTool(pi);
}
