// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_due_recall_promptsParameters = Type.Object({});

export type ListDueRecallPromptsParams = Static<
  typeof list_due_recall_promptsParameters
>;

export const list_due_recall_promptsTool: ToolDefinition<
  typeof list_due_recall_promptsParameters,
  TetherToolDetails
> = {
  name: "list_due_recall_prompts",
  label: "ListDueRecallPrompts",
  description:
    "Params for listing outstanding recall prompts.\n\nThe due list is computed over the whole live schedule, so it takes no inputs\nbeyond the session identity the gate already requires.",
  promptSnippet:
    "Params for listing outstanding recall prompts.\n\nThe due list is computed over the whole live schedule, so it takes no inputs\nbeyond the session identity the gate already requires.",
  parameters: list_due_recall_promptsParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/list_due_recall_prompts",
        name: "list_due_recall_prompts",
      },
      params,
      signal,
    );
  },
};

export function registerListDueRecallPromptsTool(pi: ExtensionAPI): void {
  pi.registerTool(list_due_recall_promptsTool);
}

export default function list_due_recall_promptsToolExtension(
  pi: ExtensionAPI,
): void {
  registerListDueRecallPromptsTool(pi);
}
