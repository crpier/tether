// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_todosParameters = Type.Object({});

export type ListTodosParams = Static<typeof list_todosParameters>;

export const list_todosTool: ToolDefinition<
  typeof list_todosParameters,
  TetherToolDetails
> = {
  name: "list_todos",
  label: "ListTodos",
  description:
    "Params for listing the active Todos, split into ready and waiting.\n\nRead-only; takes no inputs beyond the session identity the gate requires.",
  promptSnippet:
    "Params for listing the active Todos, split into ready and waiting.\n\nRead-only; takes no inputs beyond the session identity the gate requires.",
  parameters: list_todosParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/list_todos", name: "list_todos" },
      params,
      signal,
    );
  },
};

export function registerListTodosTool(pi: ExtensionAPI): void {
  pi.registerTool(list_todosTool);
}

export default function list_todosToolExtension(pi: ExtensionAPI): void {
  registerListTodosTool(pi);
}
