// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const read_gmail_messageParameters = Type.Object({
  message_id: Type.String({ minLength: 1 }),
  max_chars: Type.Optional(
    Type.Integer({ default: 50000, maximum: 200000, minimum: 1000 }),
  ),
});

export type ReadGmailMessageParams = Static<
  typeof read_gmail_messageParameters
>;

export const read_gmail_messageTool: ToolDefinition<
  typeof read_gmail_messageParameters,
  TetherToolDetails
> = {
  name: "read_gmail_message",
  label: "ReadGmailMessage",
  description: "Read one full raw RFC 2822 source text by message id.",
  promptSnippet: "Read one full raw RFC 2822 source text by message id.",
  parameters: read_gmail_messageParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/read_gmail_message",
        name: "read_gmail_message",
      },
      params,
      signal,
    );
  },
};

export function registerReadGmailMessageTool(pi: ExtensionAPI): void {
  pi.registerTool(read_gmail_messageTool);
}

export default function read_gmail_messageToolExtension(
  pi: ExtensionAPI,
): void {
  registerReadGmailMessageTool(pi);
}
