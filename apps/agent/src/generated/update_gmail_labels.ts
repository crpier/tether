// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const update_gmail_labelsParameters = Type.Object({
  add_labels: Type.Optional(Type.Array(Type.String())),
  message_id: Type.String({ minLength: 1 }),
  remove_labels: Type.Optional(Type.Array(Type.String())),
});

export type UpdateGmailLabelsParams = Static<
  typeof update_gmail_labelsParameters
>;

export const update_gmail_labelsTool: ToolDefinition<
  typeof update_gmail_labelsParameters,
  TetherToolDetails
> = {
  name: "update_gmail_labels",
  label: "UpdateGmailLabels",
  description: "Add and remove human-readable labels on one Gmail message.",
  promptSnippet: "Add and remove human-readable labels on one Gmail message.",
  parameters: update_gmail_labelsParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/update_gmail_labels",
        name: "update_gmail_labels",
      },
      params,
      signal,
    );
  },
};

export function registerUpdateGmailLabelsTool(pi: ExtensionAPI): void {
  pi.registerTool(update_gmail_labelsTool);
}

export default function update_gmail_labelsToolExtension(
  pi: ExtensionAPI,
): void {
  registerUpdateGmailLabelsTool(pi);
}
