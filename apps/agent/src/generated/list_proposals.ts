// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { StringEnum } from "@earendil-works/pi-ai";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_proposalsParameters = Type.Object({
  state: Type.Optional(
    StringEnum([
      "pending",
      "approved",
      "executing",
      "executed",
      "failed",
      "rejected",
    ] as const),
  ),
  limit: Type.Optional(Type.Integer({ default: 50, exclusiveMinimum: 0 })),
});

export type ListProposalsParams = Static<typeof list_proposalsParameters>;

export const list_proposalsTool: ToolDefinition<
  typeof list_proposalsParameters,
  TetherToolDetails
> = {
  name: "list_proposals",
  label: "ListProposals",
  description: "Params for listing proposals, newest first, capped at `limit`.",
  promptSnippet:
    "Params for listing proposals, newest first, capped at `limit`.",
  parameters: list_proposalsParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/list_proposals", name: "list_proposals" },
      params,
      signal,
    );
  },
};

export function registerListProposalsTool(pi: ExtensionAPI): void {
  pi.registerTool(list_proposalsTool);
}

export default function list_proposalsToolExtension(pi: ExtensionAPI): void {
  registerListProposalsTool(pi);
}
