// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const complete_bucket_itemParameters = Type.Object({
  bucket_item_id: Type.String({ format: "uuid" }),
  version: Type.Integer({ exclusiveMinimum: 0 }),
});

export type CompleteBucketItemParams = Static<
  typeof complete_bucket_itemParameters
>;

export const complete_bucket_itemTool: ToolDefinition<
  typeof complete_bucket_itemParameters,
  TetherToolDetails
> = {
  name: "complete_bucket_item",
  label: "CompleteBucketItem",
  description: "Params for completing a Bucket item at an observed version.",
  promptSnippet: "Params for completing a Bucket item at an observed version.",
  parameters: complete_bucket_itemParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/complete_bucket_item",
        name: "complete_bucket_item",
      },
      params,
      signal,
    );
  },
};

export function registerCompleteBucketItemTool(pi: ExtensionAPI): void {
  pi.registerTool(complete_bucket_itemTool);
}

export default function complete_bucket_itemToolExtension(
  pi: ExtensionAPI,
): void {
  registerCompleteBucketItemTool(pi);
}
