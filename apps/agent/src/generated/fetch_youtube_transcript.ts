// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const fetch_youtube_transcriptParameters = Type.Object({
  video_id: Type.String(),
});

export type FetchYouTubeTranscriptParams = Static<
  typeof fetch_youtube_transcriptParameters
>;

export const fetch_youtube_transcriptTool: ToolDefinition<
  typeof fetch_youtube_transcriptParameters,
  TetherToolDetails
> = {
  name: "fetch_youtube_transcript",
  label: "FetchYoutubeTranscript",
  description: "Params for fetching and persisting a video's transcript.",
  promptSnippet: "Params for fetching and persisting a video's transcript.",
  parameters: fetch_youtube_transcriptParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      {
        endpoint: "/internal/tools/fetch_youtube_transcript",
        name: "fetch_youtube_transcript",
      },
      params,
      signal,
    );
  },
};

export function registerFetchYoutubeTranscriptTool(pi: ExtensionAPI): void {
  pi.registerTool(fetch_youtube_transcriptTool);
}

export default function fetch_youtube_transcriptToolExtension(
  pi: ExtensionAPI,
): void {
  registerFetchYoutubeTranscriptTool(pi);
}
