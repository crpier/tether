// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const add_purchaseParameters = Type.Object({
  name: Type.String(),
  decision_factors: Type.Optional(Type.Array(Type.String())),
  intent_context: Type.Optional(Type.String()),
  price: Type.Optional(Type.String()),
  store: Type.Optional(Type.String()),
});

export type AddPurchaseParams = Static<typeof add_purchaseParameters>;

export const add_purchaseTool: ToolDefinition<
  typeof add_purchaseParameters,
  TetherToolDetails
> = {
  name: "add_purchase",
  label: "AddPurchase",
  description: "Params for capturing a purchase under consideration.",
  promptSnippet: "Params for capturing a purchase under consideration.",
  parameters: add_purchaseParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/add_purchase", name: "add_purchase" },
      params,
      signal,
    );
  },
};

export function registerAddPurchaseTool(pi: ExtensionAPI): void {
  pi.registerTool(add_purchaseTool);
}

export default function add_purchaseToolExtension(pi: ExtensionAPI): void {
  registerAddPurchaseTool(pi);
}
