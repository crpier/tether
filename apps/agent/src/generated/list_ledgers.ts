// Do not edit — generated by `just codegen`.
// Source: apps/host/tether/tools.py Pydantic params.

import type {
  ExtensionAPI,
  ToolDefinition,
} from "@earendil-works/pi-coding-agent";
import { Type, type Static } from "typebox";
import { executeTetherTool, type TetherToolDetails } from "../runtime.js";

const list_ledgersParameters = Type.Object({});

export type ListLedgersParams = Static<typeof list_ledgersParameters>;

export const list_ledgersTool: ToolDefinition<
  typeof list_ledgersParameters,
  TetherToolDetails
> = {
  name: "list_ledgers",
  label: "ListLedgers",
  description: "List current approved generic Ledgers and their field schemas.",
  promptSnippet:
    "List current approved generic Ledgers and their field schemas.",
  parameters: list_ledgersParameters,
  async execute(_toolCallId, params, signal) {
    return executeTetherTool(
      { endpoint: "/internal/tools/list_ledgers", name: "list_ledgers" },
      params,
      signal,
    );
  },
};

export function registerListLedgersTool(pi: ExtensionAPI): void {
  pi.registerTool(list_ledgersTool);
}

export default function list_ledgersToolExtension(pi: ExtensionAPI): void {
  registerListLedgersTool(pi);
}
